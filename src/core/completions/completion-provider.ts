/**
 * Completion Provider Stub
 *
 * Minimal stub for the CompletionProvider class removed during extraction.
 */

export class CompletionProvider {
  async getChangeIds(): Promise<string[]> {
    return [];
  }

  async getSpecIds(): Promise<string[]> {
    return [];
  }
}
